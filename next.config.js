/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Invitation',
  assetPrefix: '/Invitation/',
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
